export const environment = {
  production: false,
  // Ensure this URL matches the port where your .NET API is running.
  // Common .NET Core ports are http://localhost:5000, https://localhost:5001, or random ports like https://localhost:7087
  apiUrl: 'https://localhost:5001/api' 
};
